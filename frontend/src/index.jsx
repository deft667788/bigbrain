import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(
  <React.StrictMode className="w-full h-full">
    <App />
  </React.StrictMode>,
  document.getElementById('root'),
);
